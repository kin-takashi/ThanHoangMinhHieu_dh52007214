#include "Header.h"
void nhap(int a[], int n)
{
	for (int i = 0; i < n; i++)
	{
		cout << "Nhap a[" << i << "]: ";
		cin >> a[i];
	}
}
// ham xuat chan
void xuatChan(int a[]) {
	for (int i = 0; i < n; i++) {
		if (a[i] % 2 == 0) {
			cin >> a[i];
		}
	}

}